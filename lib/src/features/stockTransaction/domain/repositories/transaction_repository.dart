abstract class TransactionRepository {
  // /// Busca todos as transações de estoque
  // Future<List<StockTransaction>> fetchAll();
  // /// Busca um estoque pelo ID
  // Future<StockTransaction> fetchById(int id);
  // /// Salva várias transações
  // Future<void> saveAll(List<StockTransaction> stockTransactions);
  // /// Salva uma transação de estoque
  // Future<StockTransaction> save(StockTransaction stockTransactions);
  // /// Remove uma transação
  // Future<void> delete(StockTransaction stockTransaction);
  // /// Remove TODAS as transações
  // Future<void> deleteAll();
}